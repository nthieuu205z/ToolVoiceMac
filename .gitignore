.env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store
jobs/
bin/
web/static/previews/*.wav
web/static/previews/*.mp3
tests/fixtures/*.mp4
!tests/fixtures/.gitkeep
custom_voices/

# Thư mục skills của agent, không thuộc ứng dụng.
.Agent/

# Cấu hình Claude Code cá nhân (marketplace/MCP đã duyệt riêng máy này).
.claude/settings.local.json

# Kết quả tool phân tích code (không thuộc ứng dụng).
graphify-out/

# File audio đo/smoke ở gốc repo (sản phẩm của spike, không phải nguồn).
/*.wav

# Build artifact của Python.
*.egg-info/

# Screenshots/logs generated by local Playwright QA.
.playwright-mcp/
voice-lab-*.png

# Performance benchmark outputs are local-only and may contain environment facts.
.hermes/perf/

# Isolated Git worktrees used for feature development.
.worktrees/

# Local development tools and generated artifacts.
.hermes/
.idea/
.superpowers/
.claude/
.mcp.json
build/
dist/
qa-*.png

# All generated voice previews, including per-voice folders.
web/static/previews/
